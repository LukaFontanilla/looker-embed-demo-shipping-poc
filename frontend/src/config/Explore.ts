import { msg } from "@lingui/core/macro";

export const Explore = {
  TITLE: msg`Explore`,
  SUBTITLE: msg`Build custom visual queries across terminal doors, tractors, trailers, and LTL shipment routes.`,
};
